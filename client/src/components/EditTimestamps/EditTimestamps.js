export default function EditTimestamp({ userId }) {
  return (
    <>
      {/* removal icon next to existing timestamps - removal dialog */}
      {/* edit icon next to exsisting timestamps - timestamp dialog */}

      {/* plus button to show the type selection */}
      {/* type horizontal button list with icons text on hover or active */}
      {/* date input */}
      {/* subType dropdown if needed */}
      {/* dynamic structure by type */}
      {/* // detail text input */}
      {/* // text text input */}
      {/* subText text input */}
    </>
  );
}
